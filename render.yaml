# Render service template — fill in your repo and names before applying
services:
  - type: web
    name: parkhub-api
    env: docker
    dockerfilePath: apps/api/Dockerfile
    repo: https://github.com/Sakshamjain98/ParkHub.git
    branch: master
    buildCommand: "yarn --cwd apps/api install --frozen-lockfile && yarn --cwd apps/api build"
    startCommand: "yarn --cwd apps/api start:prod"
    envVars:
      - key: DATABASE_URL
        value: "postgresql://my_db_l5px_user:suXEZ3YLX3Tl21vzeK18iTryiKNRFZ0s@dpg-d70i5iqa214c73eaj640-a.oregon-postgres.render.com/my_db_l5px"
      - key: REDIS_URL
        value: "redis://default:AZVrAAIncDFlOTZiMzU5OTAwZDM0MTQ5Yjc0MGYwYTI4ZTNlMTIzYXAxMzgyNTE@exotic-muskox-38251.upstash.io:6379"

databases:
  - type: postgres
    name: parkhub-db
    plan: starter
    region: oregon

# Note: either create these services from the Render dashboard or adapt this
# template for render.yaml usage. You'll need to supply repository, build
# commands and any environment variables (DATABASE_URL, etc.).

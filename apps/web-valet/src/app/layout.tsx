import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '@ParkHub/ui/src/app/globals.css'
import { MenuItem } from '@ParkHub/util/types'
import { ApolloProvider } from '@ParkHub/network/src/config/apollo'
import { SessionProvider } from '@ParkHub/ui/src/components/molecules/SessionProvider'
import { ToastContainer } from '@ParkHub/ui/src/components/molecules/Toast'
import { Container } from '@ParkHub/ui/src/components/atoms/Container'
import { Header } from '@ParkHub/ui/src/components/organisms/Header'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ParkHub | Valet',
  description: 'Generated by create next app',
}

const MENUITEMS: MenuItem[] = [{ label: 'My Trips', href: '/my-trips' }]

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-gray-25`}>
        <SessionProvider>
          <ApolloProvider>
            <Header type="valet" menuItems={MENUITEMS} />
            <Container>{children}</Container>
          </ApolloProvider>
        </SessionProvider>
        <ToastContainer />
      </body>
    </html>
  )
}
